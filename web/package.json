{
    "scripts": {
        "start:server": "json-server -w db.json",
        "start:dev": "parcel index.html",
        "start": "concurrently yarn:start:*"
    },
    "devDependencies": {
        "typescript": "^3.5.3"
    },
    "dependencies": {
        "axios": "^0.19.0",
        "concurrently": "^4.1.2",
        "json-server": "^0.15.0"
    }
}
